/*
The newly created time object
has the current time, which
is automatically converted to
a string when passing to print().

(Similar to Java's toString() method)
*/

while(true) {
    print(new Date());
    sleep(1000);
}
